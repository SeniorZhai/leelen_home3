services:
  homeassistant:
    image: ${LEELEN_IMAGE:?Set LEELEN_IMAGE to the verified image tag or digest}
    container_name: leelen-homeassistant
    restart: unless-stopped
    network_mode: host
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./config:/config
